services:
  postgres:
    container_name: routine_revo_pg
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=routine_revo_db'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - "5432:5432"
